import Navbar from "@/components/navbar/navbar";

export default function WebcamLayout({ children }) {
    return <section>
        {/* <Navbar webcam={false}/> */}
        {children}
        </section>
  }